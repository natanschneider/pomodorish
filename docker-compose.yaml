services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tanstack-app
    environment:
      - NODE_ENV=production
      - PORT=3001
    ports:
      - "3001:3001"
    restart: unless-stopped
    networks:
      - tanstack-network

networks:
  tanstack-network:
    driver: bridge
