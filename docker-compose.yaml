services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pomodorish-app
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - pomodorish-network

networks:
  pomodorish-network:
    driver: bridge
